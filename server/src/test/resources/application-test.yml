lighter:
    session:
        permanent-sessions:
            - id: "permanentId1"
              submit-params:
                  conf:
                      "spark.kubernetes.namespace": "spark"
                      "spark.kubernetes.driver.secrets.spark-secret": "/etc/secret"
