name: build backend and frontend
on:
  push:
    tags-ignore:
      - "*"

jobs:
  build-frontend:
    uses: .github/workflows/build-frontend.yml@main
  build-java:
    uses: .github/workflows/build-java.yml@main
