name: build backend and frontend
on:
  push:
    branches:
      - "*"
    tags-ignore:
      - "*"

jobs:
  build-frontend:
    uses: ./.github/workflows/build-frontend.yml@master
  build-java:
    uses: ./.github/workflows/build-java.yml@master
