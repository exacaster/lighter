name: publish docker image
on:
  release:
    types:
      - released

jobs:
  build-frontend:
    uses: .github/workflows/workflow_callable/build-frontend.yml@main
  build-java:
    uses: .github/workflows/workflow_callable/build-java.yml@main
  publish-registry:
    uses: .github/workflows/workflow_callable/publish-registry.yml@main
    needs: [build-frontend, build-java]
