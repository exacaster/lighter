name: publish docker image
on:
  release:
    types:
      - released

jobs:
  build-frontend:
    uses: exacaster/lighter/.github/workflows/build-frontend.yml@issues_4
  build-java:
    uses: exacaster/lighter/.github/workflows/build-java.yml@issues_4
  publish-registry:
    uses: exacaster/lighter/.github/workflows/publish-registry.yml@issues_4
    needs: [build-frontend, build-java]
