name: publish docker image
on:
  release:
    types:
      - released

jobs:
  build-frontend:
    uses: exacaster/lighter/.github/build-frontend.yml@main
  build-java:
    uses: exacaster/lighter/.github/build-java.yml@main
  publish-registry:
    uses: exacaster/lighter/.github/publish-registry.yml@main
    needs: [build-frontend, build-java]
